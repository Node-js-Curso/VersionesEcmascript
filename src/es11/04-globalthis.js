console.log(window); // In a browser environment, window is the global object
console.log(global); // In Node.js, global is the global object

console.log(self); // In a web worker, self is the global object

console.log(globalThis); // globalThis is the standard way to access the global object in any environment

