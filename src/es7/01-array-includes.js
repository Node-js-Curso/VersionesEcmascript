let numbers = [1, 2, 3, 4, 5];
console.log(numbers.includes(3)); // true
console.log(numbers.includes(6)); // false

const list = ['juan', 'pedro', 'maria', 'luisa'];
console.log(list.includes('maria')); // true 